<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XFNutsAndBolts"
             xmlns:vm="clr-namespace:XFNutsAndBolts.ViewModels"
             BindingContext="{Binding Main, Source={StaticResource Locator}}"
             x:Class="XFNutsAndBolts.MainPage">

    <StackLayout VerticalOptions="CenterAndExpand">
        <!-- Place new controls here -->
        <Label Text="Welcome to Xamarin.Forms!" HorizontalOptions="Center" />
        <Label Text="{Binding Name}" HorizontalOptions="Center" />
        <Label Text="{Binding NameInJapanese}" HorizontalOptions="Center" />
        <Label Text="{Binding AppVersion}" HorizontalOptions="Center" />

        <StackLayout Margin="10">
            <Label x:Name="lblScanning" HorizontalOptions="Center" HorizontalTextAlignment="Center" TextColor="Red"
                   Text="You able to do anything on the UI while the Text above is loading. No lagging animation." />
        </StackLayout>
    </StackLayout>

</ContentPage>
